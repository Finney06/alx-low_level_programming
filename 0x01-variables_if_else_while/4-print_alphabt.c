#include <stdio.h>
/**
*	main - Entry
*	Description: 'Task 4'
*	Return: Always 0 (Success)
**/
int main(void)
{
	char letter = 'a';

	while (letter <= 'z')
{
	if (letter != 'e' && letter != 'e')
	putchar(letter);
	letter++;
}
	putchar('\n');
	return (0);
}
